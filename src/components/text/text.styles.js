import styled from 'styled-components';

const StyledText = styled.p`
	color: #f2f2f2;
	/* font-size: ${({ $big }) => ($big ? '3rem' : '1rem')}; */
	position: absolute;
	font-size: 15px;
	width: 220px;
	margin-top: 210px;
	margin-left: 50px;
`;

export { StyledText };
