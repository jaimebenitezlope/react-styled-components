import styled from 'styled-components';

const StyledTitle = styled.h1`
	color: #f2f2f2;
	/* font-size: ${({ $big }) => ($big ? '3rem' : '1rem')}; */
	position: absolute;
	font-size: 30px;
	margin-left: 48px;
	margin-top: 130px;
`;

export { StyledTitle };
